# External security-scanner feature parity — property-level crosswalk

This document inventories every distinct security property implemented by
an external reference AI-skill security scanner (out-of-tree reference
clone used only for local research, not part of this repository) and
records skil's current coverage state for each one. It is a
research/diagnostic artifact for deciding what to build next — it is
**not** a rule-ID parity claim, and it is **not** a to-do list with
prescribed implementations. Consistent with this repository's policy of
independent product identity, this document identifies the reference tool
generically rather than by name; its external rule IDs (`P1`, `AS2`, `TP3`,
etc.) are cited as-is since they are not themselves third-party branding.

Two skil documents already carry partial versions of this analysis and are
referenced rather than duplicated:

- [`external-control-crosswalk.md`](external-control-crosswalk.md) — a
  conformance-suite-backed crosswalk for ~20 external IDs skil's test suite
  actively asserts equivalence against (`AST1-9`, `TT2-5`, `LP3`, `RP1`,
  `E1`, `E2`, `EA1`, `MP2`, `PE3`, `P1`, `SC1-7`, `TM1-3`, `YR4`). Where this
  document's finding matches that file's, the coverage state and analyzer
  are carried forward unchanged; this document adds ~70 additional
  properties that crosswalk does not cover, plus the `AR`, `E3-E5`, `OH`,
  `PE1/2/4/5`, `RA`, `SSRF`, `TR`, `AS`, `TP`, `SDI`, `SQP`, and `SSD`
  families.
- [`security-control-matrix.md`](security-control-matrix.md) — skil's own
  44-control public matrix, organized around skil's trust-boundary model
  rather than the reference scanner's rule IDs. It is the source for several
  `SKIL-*` → property mappings used below.
- [`native-capabilities.md`](native-capabilities.md) — narrative description
  of skil's native capability layers.

Methodology: every analyzer source file in the external reference scanner's
static/behavioral/MCP analyzer package was read in full and every distinct
rule ID extracted with its detection logic. The skil side was built by
grepping `"SKIL-` across `internal/analyzer/*.go` (excluding tests) and
reading the implementation (`pattern.go`, `boundary.go`, `unicode.go`,
`mcp.go`, `python_ast.go`, `structured_ast.go`, `taint.go`, `dependency.go`,
`dependency_inventory.go`, `yara.go`, `local_semantic.go`, `rules.go`)
behind every rule cited here. Coverage claims below are based on comparing
the actual regex/AST/taint logic on both sides, not rule-name similarity.

Coverage states: `FULL` | `PARTIAL` | `MISSING` | `DIFFERENT_BY_DESIGN` | `NOT_APPLICABLE`

---

## 1. Prompt injection

| Reference | Property | the reference scanner approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `static_patterns_prompt_injection.py` P1 — Instruction Override | "Ignore previous instructions", override safety/security, jailbreak-mode framing | Regex, 11 patterns, per-line | `SKIL-PI-001` | FULL | — | Documented in `external-control-crosswalk.md`. skil's regex is broader (verb set `ignore/disregard/override/nullify/discard/void/supersede`) and has an explicit negative-match guard against "never ignore..." false positives that P1 lacks. |
| P1 (extended) — split-line evasion | Instruction-override phrase split across a line break to dodge single-line regex | N/A (not detected) | `overrideActionTail`/`overrideTargetHead` in `pattern.go` | skil-only, no the reference scanner equivalent | — | skil has capability the reference scanner does not. |
| P2 — Hidden Instructions (comments) | HTML comments `<!--...-->` and Markdown comments `[//]: #(...)` containing system/instruction/ignore/POST/GET keywords, restricted to markdown/other file types | Regex | none found | MISSING | skil has no HTML/Markdown-comment-scoped hidden-instruction detector; `SKIL-UNI-*` only fires on genuinely invisible Unicode, not on a visible-but-suppressed-in-rendering HTML comment | Grepped `internal/analyzer/*.go` for `<!--` / `[//]:` — no matches outside comments-about-code. |
| P2 — Hidden Instructions (zero-width / bidi override) | Zero-width chars (U+200B/C/D/2060/FEFF), bidi override chars (U+202A-E, U+2066-9), data URIs | Regex, with an emoji-ZWJ carve-out | `SKIL-UNI-001` | FULL | — | Both sides special-case legitimate ZWJ emoji sequences. |
| P2 (extended) — Unicode Tag block ASCII smuggling | Invisible U+E0000-E007F "tag" characters carrying a hidden ASCII payload, with an RGI emoji-flag carve-out | Regex + codepoint-range walk | `SKIL-UNI-003` | FULL | — | skil's `decodeUnicodeTags` mirrors the tag-block decode/carve-out logic. |
| P3 — Exfiltration Commands | NL instructions to send/transmit conversation/context/data to an external sink, "silently/secretly send" | Regex, 7 patterns | `SKIL-EX-001`, `SKIL-INTENT-EXTERNAL-TRANSFER`, `SKIL-INTENT-UNDISCLOSED-OPERATION` | PARTIAL | skil's `SKIL-EX-001` requires a *sensitive-data noun* (secret/credential/api key/env var/conversation context) combined with a sink verb; the reference scanner's P3 also fires on generic "conversation/context/history" transmission without a secrecy qualifier. The "silently/secretly send" framing gap noted below is now closed by `SKIL-INTENT-UNDISCLOSED-OPERATION` (added for SQP-2), which fires on "send ... secretly"/"transmit ... without telling the user" regardless of whether the payload noun matches `SKIL-EX-001`'s sensitive-data list. | Generic "conversation/context transmission with no secrecy qualifier at all" is still not covered — that remains a real, separate gap from the silent-framing one. |
| P4 — Behavior Manipulation | Subtle steering instructions: "always recommend X over Y", "never tell the user", "prioritize X over safety", "gain the user's trust then..." | Regex, 9 patterns | `SKIL-INTENT-BEHAVIOR-MANIPULATION` | FULL | — | New rule combining P4's sub-patterns (biased recommendation steering, silent information suppression, safety/privacy deprioritization, covert influence, trust-then-exploit framing, deceptive "appear X while Y" framing) with a negation guard so explicit refusals to steer/manipulate/exploit trust do not fire. |
| P5 — Harmful Content Injection (`static_patterns_harmful_content.py`) | Named toxic substances injected into recipes/instructions, self-harm encouragement, weapon-building instructions, with instructional/educational/warning context adjustment | Regex over a curated substance list + contextual confidence adjustment (recipe context ↑, encyclopedic/warning context ↓) | `SKIL-ABUSE-MALWARE`, `SKIL-ABUSE-PHISHING`, `SKIL-ABUSE-DESTRUCTION`, `SKIL-ABUSE-EVASION`, `SKIL-ABUSE-EXHAUSTION` | PARTIAL | skil's `ABUSE-*` family targets cyber-harm objectives (malware, phishing, destructive/irreversible actions, control evasion, resource exhaustion) with a defensive negative-match guard; none target physical/chemical harm (poisoning food, self-harm encouragement, weapon-building instructions) the way P5 does | Different threat model: skil is scoped to software/agent-abuse harms, the reference scanner's P5 targets physical-world harm content embedded in a recipe/instruction skill. |

## 2. Anti-refusal / guardrail nullification

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `static_patterns_anti_refusal.py` AR1 — Refusal suppression | "Never refuse", "always comply", "no request is off-limits" | Regex, 9 patterns, code-example confidence penalty | `SKIL-INTENT-REFUSAL` | FULL | — | Both use a negative/context guard against "must not always comply" style negations; skil's guard is regex-based (context line), the reference scanner's is a confidence penalty for code-example context — different mechanism, same net effect. |
| AR2 — Disclaimer/caveat suppression | "No warnings", "don't moralize", "skip the ethics" | Regex, 6 patterns | `SKIL-INTENT-WARNING` | FULL | — | Matches closely; both suppress false positives from a "detect/reject X" meta-discussion context. |
| AR3 — Safety/policy nullification | "You have no restrictions", "DAN mode", "not bound by any policy" | Regex, 8 patterns, with a security-review-context carve-out (e.g. "must not enable god mode" documentation) | `SKIL-INTENT-GUARDRAIL` | FULL | — | skil also has `SKIL-GUARDRAIL-I18N-001` for Chinese/Japanese/Korean phrasing of the same property, which the reference scanner's AR3 does not attempt (the reference scanner is English-only across all static-pattern analyzers). |
| — (no the reference scanner equivalent) | Multilingual (CJK) instruction override / secret exfiltration / guardrail nullification | N/A | `SKIL-PI-I18N-001`, `SKIL-EX-I18N-001`, `SKIL-GUARDRAIL-I18N-001` | skil-only | — | skil detects the same three high-value properties (instruction override, secret exfiltration, guardrail nullification) phrased in Chinese/Japanese/Korean; the reference scanner has no non-English static patterns anywhere in the analyzer set. |

## 3. Data exfiltration (incl. cloud storage)

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `static_patterns_data_exfiltration.py` E1 — External Transmission | HTTP POST/PUT calls (`requests.post`, `curl -d`, `fetch(...POST)`), suspicious `api./telemetry./collect.` subdomains | Regex over code + NL instructions | `SKIL-NET-001`, `SKIL-INTENT-EXTERNAL-TRANSFER` | FULL | — | Documented in `external-control-crosswalk.md`. `SKIL-NET-001` is AST-anchored (Python call-name resolution) rather than regex, which is a strictly stronger match than E1's regex. |
| E2 — Env Variable Harvesting | Iterating `os.environ.items()`, reading `*_KEY/SECRET/TOKEN/PASSWORD` env vars, `process.env` bulk reads, `env \| grep` | Regex | `SKIL-SEC-001` | FULL | — | Documented in crosswalk. skil's version also fires on Python AST reads (`os.environ.get(...)`), not just regex. |
| E3 — File System Enumeration | Recursive `os.walk`/`glob`/`find` scans of home dir or `.ssh`/`.aws`/`.config` looking for credential files | Regex, 11 patterns | `SKIL-INTENT-FS-DISCOVERY` (NL) + new `SKIL-FS-DISCOVERY-CODE` (code) | FULL | — | New rule matches `glob.glob(...)`, `os.walk(...)`, `Path.home().glob/rglob(...)`, and `os.listdir(...)` calls whose argument targets a home or credential directory (`.env`/`.ssh`/`.aws`/`.config`/`.kube`/`.docker`/`.gnupg`/`home`/`~`), scoped to the call's own argument list so an ordinary project-relative glob (`glob.glob("./config/*.yaml")`) does not fire. |
| E4 — Context Leakage | NL/code instructions to send/log/export the full conversation, session, or agent memory to an external destination | Regex, 9 patterns | `SKIL-EX-001` (partial), `SKIL-INTENT-EXTERNAL-TRANSFER` (partial) | PARTIAL | Neither skil rule specifically targets "conversation/session/memory" as the exfiltrated object the way E4 does; both require either a named-secret noun or a bare "send to URL" pattern | — |
| E5 — Cloud Storage Exfiltration | boto3 `put_object`/`upload_file`, `aws s3 cp/sync`, `gsutil cp/rsync`, google-cloud-storage `upload_from_*`, `az storage blob upload`, Azure `upload_blob` SDK call — filtered via code-example suppression | Regex, 8 patterns, low confidence by design | `SKIL-BOUNDARY-CLOUD-EXFIL` (CLI form) + new `SKIL-BOUNDARY-CLOUD-SDK-UPLOAD` (SDK form) | FULL | — | New rule matches the bare SDK method names (`put_object`, `upload_fileobj`, `upload_file`, `upload_from_filename`, `upload_from_string`, `upload_from_file`, `upload_blob`) without requiring a co-occurring secret keyword, mirroring the reference scanner's own low-confidence-by-design choice; kept as a separate Medium-severity rule from the Critical CLI+secret-evidence `SKIL-BOUNDARY-CLOUD-EXFIL` since the evidence strength differs. |

## 4. Privilege escalation

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `static_patterns_privilege_escalation.py` PE1 — Excessive Permissions | `permissions: ["*"]`, "grant me full access" | Regex | `SKIL-AGENCY-TOOLS`, `SKIL-MCP-001` | PARTIAL | skil's wildcard detection is scoped to MCP tool/permission JSON structures and to "use any/all tools" phrasing; PE1's broader "request full/complete access" NL phrasing outside an MCP-shaped block is not explicitly matched | — |
| PE2 — Sudo/Root Execution | `sudo`, `su -`, `doas`, `pkexec`, `chmod +s`, with a documentation-example suppressor | Regex, 13 patterns | `SKIL-SH-002` | FULL | — | `SKIL-SH-002` (defined in `code.go`, not `structured_ast.go` as originally suspected) previously matched only bare line-leading `sudo`; extended to also match `doas`, `pkexec`, `su -`, and `chmod u+s`/`chmod +s`/octal setuid modes (`4xxx`), while leaving ordinary `chmod +x`/`chmod 644` and prose mentions of "su" unaffected. |
| PE3 — Credential Access | `.ssh/id_rsa`, `.aws/credentials`, `.kube/config`, `/etc/shadow`, browser cookie/login-data files, keychain access — with read-only Docker-mount and documentation-example suppression | Regex, 24 patterns | `SKIL-SEC-001` | FULL | — | Path list extended: `.kube/config`, `.docker/config.json`, `.npmrc`, `.git-credentials`, `.netrc`, `/etc/shadow`, GCP (`.config/gcloud/*credentials*.json`) and Azure (`.azure/credentials`, `.azure/accessTokens.json`) credential paths, `.gnupg/`, and browser login-data/keychain/cookie access (scoped to "browser ..." phrasing to avoid colliding with ordinary UI-copy prose like "Login Data form"). |
| PE4 — Docker Socket Access | `/var/run/docker.sock`, `docker.from_env()`, `DockerClient(...)`, `http+unix://...docker.sock` | Regex, best-confidence-per-line dedup | `SKIL-BOUNDARY-CONTAINER` | FULL | — | skil also covers `containerd.sock` and the Kubernetes service-account token path, which PE4 does not. |
| PE5 — Privileged Container / Container Escape | `--privileged`, `-v /:`, `--cap-add=SYS_ADMIN`, `--pid=host`, `nsenter`, cgroup `release_agent` escape, `/proc/N/ns/` | Regex, 10 patterns, code-example filtered | `SKIL-BOUNDARY-CONTAINER-ESCAPE` | FULL | — | Extended to also match `--cap-add=SYS_ADMIN`/`SYS_PTRACE`/`NET_ADMIN`, `nsenter --target/--mount/--uts/--ipc/--net/--pid`, `unshare --user/--mount/--pid/--uts/--net`, and the cgroup `release_agent` breakout (both `release_agent=` and a `/sys/fs/cgroup/.../release_agent` path write). `/proc/N/ns/` namespace-file access specifically was not added (judged too generic/high-noise on its own without a co-occurring escape primitive). |

## 5. Excessive agency

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `static_patterns_excessive_agency.py` EA1 — Unrestricted Tool Access | Wildcard tool permissions, "call any/all tools", "unrestricted tool access" | Regex, 10 patterns | `SKIL-AGENCY-TOOLS`, `SKIL-MCP-001` | FULL | — | Documented in crosswalk. |
| EA2 — Autonomous Decision Making | "Without asking confirmation", "auto-approve", "proceed without waiting for permission" — code-example filtered | Regex, 9 patterns | `SKIL-AGENCY-APPROVAL` | FULL | skil's pattern is narrower (requires a specific high-impact verb — deploy/delete/purchase/publish/send/modify/proceed — paired with "without approval") vs EA2's broader "skip/bypass/disable confirmation" set with no verb requirement | Marked FULL because the core property (approval-bypass instruction) is covered, but skil's verb-anchoring means some EA2-style generic "don't ask for confirmation" phrasing without a named action verb would slip through. Borderline PARTIAL. |
| EA3 — Scope Creep | "While you're at it, also...", "extend your scope", "act as a general-purpose/universal assistant" | Regex, 8 patterns | none found | MISSING | No skil rule targets self-declared scope expansion beyond a skill's stated purpose via static pattern; skil's closest analog (`SKIL-INTENT-SCOPE`, `SKIL-INTENT-CONTEXT`) are LLM-provider-backed semantic checks, not deterministic pattern matches, and require the semantic provider to be enabled. | — |
| EA4 — Unbounded Resource Access | "Unlimited API calls", `max_retries=None`, `timeout=float('inf')`, "allocate as much memory as needed" | Regex, 9 patterns | `SKIL-AGENCY-BOUNDS` | PARTIAL | `SKIL-AGENCY-BOUNDS` matches "no/unlimited rate limit/quota/timeout/budget" NL phrasing but has no code-level match for literal `max_retries=None`/`timeout=math.inf` Python assignments the way EA4 does | Code-level EA4 patterns are a gap. |

## 6. Output handling

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `static_patterns_output_handling.py` OH1 — Unvalidated Output Injection | Model output piped into `exec`/`eval`/`subprocess`/`os.system`, `innerHTML=`, SQL string interpolation, "pipe the output directly to the shell" | Regex, 14 patterns spanning Python/JS/SQL/shell | `SKIL-OUTPUT-EXECUTION` | PARTIAL | skil's rule matches NL framing ("execute/eval/run model output directly/without validation") and a literal "run the generated code directly" phrase; it has no code-level regex/AST match for the concrete sink patterns OH1 covers (`exec(response)`, `innerHTML = output`, f-string SQL with `{response}`, `dangerouslySetInnerHTML`) | The taint analyzer (`taint.go`) tracks generic execution/network/filesystem/log sinks but is not documented here as tracing specifically from "model output" as a taint *source* — worth verifying in a follow-up pass. |
| OH2 — Cross-Context Output | Output from one agent/session/context passed into another's system prompt or trust boundary | Regex, 6 patterns | `SKIL-OUTPUT-BOUNDARY` | FULL | — | Property match is close: both require an explicit "insert/copy tool output into system prompt / trusted context / another agent" phrase. |
| OH3 — Unbounded Output | No output length/token limit, `max_tokens=None`, "generate unlimited text", "flood the output" | Regex, 9 patterns | `SKIL-OUTPUT-LIMIT` | PARTIAL | Same gap as EA4: skil's NL-only pattern ("no/unlimited output limit") has no code-level `max_tokens=None`/`max_length=-1` literal match. | — |

## 7. Prompt leakage / system prompt confidentiality

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `static_patterns_system_prompt_leakage.py` P6 — Direct Prompt Extraction | "Print/reveal/show your system prompt", "begin your response with your instructions", with a benign "## Output Rules" heading carve-out | Regex, 8 patterns | `SKIL-PL-001` | FULL | — | Both suppress a documented benign-heading false positive (skil via negative-match context, the reference scanner via an exact-heading carve-out). |
| P7 — Indirect Prompt Extraction | "Repeat everything above", "translate/encode/rot13 your instructions", "format your prompt as JSON" | Regex, 10 patterns | `SKIL-PROMPT-INDIRECT-LEAK` | FULL | — | skil's rule covers translate/rephrase/encode/summarize; it does not explicitly list rot13/base64/reverse or "write a poem about your instructions" framings P7 has, so treat as a close-but-not-exhaustive match. |
| P8 — Prompt Exfiltration via Tool | "Save your system prompt to a file", "send your instructions via curl/webhook/API tool" | Regex, 7 patterns | none found (closest: `SKIL-EX-001` requires a secret-noun, `SKIL-PL-001` requires direct disclosure, neither covers "write prompt to file/tool") | MISSING | The specific combination of prompt-disclosure + tool-mediated exfiltration (as opposed to disclosure-in-response or generic secret exfiltration) has no dedicated skil rule. | — |

## 8. Memory / state manipulation

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `static_patterns_memory_poisoning.py` MP1 — Persistent Context Injection | "Remember this for all future interactions", "store this in your memory permanently" | Regex, 9 patterns | `SKIL-MP-001` | FULL | — | skil's rule is a superset in some ways (also matches "modify agent memory" directly) but both target the same persist-across-sessions property. |
| MP2 — Context Window Stuffing | Long repeated-substring detection (regex backreference `(.{2,20}?)\1{20,}`), "fill/pad/stuff the context window", "displace the original instructions" | Regex including a repeated-content detector | `SKIL-MEMORY-SATURATION` | FULL | — | Documented in crosswalk. skil requires explicit "fill/stuff/flood/pad ... displace/push out/override" phrasing; it does **not** replicate MP2's structural repeated-substring detector (`(.{2,20}?)\1{20,}`), so a skill file that is *actually* padded with repeated junk text (without saying so) would be caught by the reference scanner but not skil. |
| MP3 — Memory Manipulation | "Clear/wipe your memory", "overwrite your instructions", "inject false memories", "you are no longer an assistant" — code-example filtered | Regex, 10 patterns | `SKIL-AGENT-SELF-MODIFY` (partial overlap), `SKIL-MP-001` (partial overlap) | PARTIAL | Neither skil rule covers the "clear/reset/wipe memory", "you are no longer X", or "inject false/fabricated memories" phrasings verbatim; `SKIL-AGENT-SELF-MODIFY` is scoped to code/config self-modification, not memory-content manipulation | — |

## 9. Rogue agent / self-modification / persistence

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `static_patterns_rogue_agent.py` RA1 — Self-Modification | `open(__file__, 'w')`, "modify your own code/config", "disable the safety check" | Regex, 11 patterns | `SKIL-AGENT-SELF-MODIFY` | FULL | — | skil's rule matches "modify/rewrite/patch/update your own/its own code/instructions/configuration/policy/skill.md"; RA1's direct-file-write-to-`__file__` code pattern is not independently matched but the NL/instruction form is. |
| RA2 — Session Persistence | crontab, `.bashrc`/`.zshrc` startup injection, systemd/launchd service install, background/detached process spawning, Windows registry/plist persistence | Regex, 15 patterns | `SKIL-PERSISTENCE-STARTUP` | PARTIAL | skil matches "install/create/add cron/crontab/startup/launch agent/systemd/autorun/login item" but has no equivalent for `.bashrc`/`.zshrc` append, `nohup`/`disown`/`setsid` background detachment, or Windows registry (`HKEY_`/`reg add`) / `defaults write`/`launchctl load` persistence primitives RA2 covers | — |

## 10. Trigger abuse

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `static_patterns_supply_chain.py` TR1 — Overly Broad Trigger | Single common-word trigger ("the", "do", "help") or trigger ≤2 chars, from the manifest `triggers` field | Curated word-list + length check against parsed manifest | `SKIL-TRIGGER-GENERIC` | PARTIAL | skil's rule is regex-over-text ("trigger/activate/invoke ... help/code/file/task/question/please" or "every/any/all message/request/input") applied to prose, not a structured check against a parsed `triggers:` manifest array the way TR1 is; a manifest with `triggers: ["do"]` and no narrative sentence naming it would not be flagged by skil | Structural (manifest-field-aware) vs textual (prose-regex) — a meaningful mechanism difference, not just wording. |
| TR2 — Shadow Command Trigger | Trigger string collides with a built-in command name (`help`, `commit`, `deploy`, ...) from a curated 50-word list | Set-membership check against parsed manifest triggers | `SKIL-TRIGGER-SHADOW` | PARTIAL | skil's rule matches NL phrasing ("shadow/replace/override/intercept a built-in command/skill/tool") rather than doing a structural set-membership check of the manifest's `triggers` array against a builtin-command list the way TR2 does | Same structural-vs-textual gap as TR1. |
| TR3 — Keyword Baiting Trigger | Trigger is "anything/everything/whatever/always" or "when the user says anything" — from parsed manifest | Regex against parsed manifest triggers | `SKIL-TRIGGER-GENERIC` (overlaps) | PARTIAL | Same structural gap; skil's generic-trigger rule works on prose text, not the parsed manifest `triggers:` field directly. | — |

## 11. Dynamic code execution (Python)

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `behavioral_ast.py` AST1-9 (exec/eval/`__import__`/subprocess/os-exec-family/compile/dynamic-getattr/dangerous-chain/reflective-getattr) | Python AST walk over 9 dangerous-call shapes, including a chain detector (`exec(base64.b64decode(...))`) and a literal-sink-name reflective-getattr detector | Python `ast` module, full syntax-tree walk | `SKIL-PY-001`–`004`, `SKIL-PY-REFLECT-EXEC`, `SKIL-TAINT-EXECUTION` | FULL | — | All 9 documented FULL in `external-control-crosswalk.md`. |

## 12. Dynamic code execution (JavaScript/TypeScript and shell)

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| — (no dedicated the reference scanner analyzer) | JS/TS dynamic execution (`eval`, `new Function`, `child_process.exec`) | Only caught incidentally by SC2/SC3/OH1 regex, not a dedicated AST pass | `SKIL-JS-001`, `SKIL-JS-002` (Tree-sitter AST, per `native-capabilities.md`) | skil-only, no the reference scanner AST equivalent | — | the reference scanner has **no** JS/TS AST analyzer at all — `behavioral_ast.py` is Python-only. Its JS coverage is limited to the regex patterns embedded in `static_patterns_output_handling.py` (OH1) and `static_patterns_supply_chain.py` (SC2/SC3), which match literal strings like `eval(atob(...))` but do not parse JS syntax. skil's Tree-sitter-based JS/TS/TSX AST analyzer is a capability skil has that the reference scanner structurally lacks. |
| — (no dedicated the reference scanner analyzer) | Shell dynamic evaluation, remote-script pipelines, privilege escalation, dangerous recursive removal | Only caught by regex in `static_patterns_supply_chain.py` (SC2), `static_patterns_tool_misuse.py` (TM1/TM2), `static_patterns_privilege_escalation.py` (PE2) | `SKIL-SH-001`–`004` (Tree-sitter Bash AST) | skil-only, no the reference scanner AST equivalent | — | Same story as JS: the reference scanner has no Bash-syntax AST pass; skil's Tree-sitter Bash analyzer parses shell syntax rather than regexing shell text, catching e.g. quoted/escaped variants of `rm -rf` or `curl \| sh` a regex can miss. |

## 13. Taint tracking (source → sink data flow)

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `behavioral_taint_tracking.py` TT1 — Direct source→sink flow (generic) | Any source (env/file/network/user-input) flowing directly into any sink (network/exec/file-write), lowest-specificity fallback rule | Python AST, direct-call-argument matching | skil's taint analyzer (`taint.go`) fallback | PARTIAL/DIFFERENT_BY_DESIGN | skil's taint engine has 4 named sink categories (network, execution, filesystem-write, log) vs the reference scanner's 3 (network-output, exec, file-write) plus a generic TT1/TT2 fallback for source→sink pairs that don't hit a named specific rule (TT3/TT4/TT5); it is not confirmed skil emits a finding for every generic source→sink combination the way TT1's fallback does, only for the categories it names | Needs code-level confirmation; flagged rather than asserted. |
| TT2 — Variable-mediated (indirect) taint | Taint propagated through re-assignment, container literals (dict/list/tuple), and f-strings before reaching a sink | Python AST with an explicit `tainted` variable map | `SKIL-TAINT-*` | FULL | — | Documented FULL in crosswalk ("multi-step assignment aliases are propagated"). |
| TT3 — Credential → network (critical) | `os.environ`/`os.getenv` source flowing to `requests.post`/`socket.send` etc. | AST source/sink pairing | `SKIL-TAINT-NETWORK` | FULL | — | Documented FULL in crosswalk. |
| TT4 — File read → network | `open()`/`Path.read_text()` source flowing to a network sink | AST source/sink pairing | `SKIL-TAINT-NETWORK` | FULL | — | Documented FULL in crosswalk; skil folds this into the same rule ID as TT3 rather than a distinct one — a rule-ID difference, not a property gap. |
| TT5 — External input → execution (critical) | `requests.get`/`input()`/stdin source flowing into `exec`/`eval`/`subprocess` | AST source/sink pairing | `SKIL-TAINT-EXECUTION` | FULL | — | Documented FULL in crosswalk. |
| — (no the reference scanner equivalent) | Taint reaching logging/print sinks | N/A | `SKIL-TAINT-LOG` | skil-only | — | the reference scanner's `_FILE_WRITE_SINKS`/`_NETWORK_OUTPUT_SINKS`/`_EXEC_SINKS` have no logging-sink category; skil's taint engine adds one (`print`/`console.log`/`logger.*` as sinks), catching secret-to-log leakage the reference scanner's taint tracker structurally cannot. |

## 14. Obfuscation / dependency-hiding

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `static_patterns_supply_chain.py` SC3 — Obfuscated Code | `exec(base64.b64decode(...))`, `marshal.loads`, hex/base64-decode-then-execute chains, `_0x` minified-var naming, long base64/hex string literals | Regex, 16 patterns | `SKIL-OBF-001`, `SKIL-UNI-*` | FULL | — | Documented FULL in crosswalk. |
| SC2 — External Script Fetching and Execution | `curl \| sh`, `wget \| python`, `eval(fetch(...))`, with a trusted-domain (nodesource/docker/pypi/npmjs/github) allowlist | Regex + hostname-token trust-domain check | `SKIL-SH-001` | FULL | — | Documented FULL in crosswalk. |

## 15. Unicode deception

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `mcp_tool_poisoning.py` TP2 — Homoglyph identifiers | Cyrillic/Greek lookalike chars (а/e/о/р/с/у Cyrillic → a/e/o/p/c/y Latin) substituted into MCP tool/param *identifier* fields | Curated confusables map, applied to identifier-typed manifest fields only | `SKIL-UNI-002` | PARTIAL | skil's confusable-hostname rule (`SKIL-UNI-002`) is described as targeting hostname-like tokens mixing Latin/Cyrillic, not specifically MCP tool/parameter *identifier* names; whether it also fires on a poisoned tool name (not a hostname) was not confirmed by reading `unicode.go`'s `mixedScriptToken` logic in full | Needs closer code read to confirm scope overlap; flagged PARTIAL out of caution rather than asserted FULL. |
| TP2 — RTL/directional override | U+202E, U+202D, U+2066-9 anywhere in MCP metadata text (not just identifiers) | Regex | `SKIL-UNI-001` | FULL | — | Same codepoint range as skil's general bidi-override detector. |
| TP2 — Invisible formatting chars in identifiers | Soft hyphen U+00AD, CGJ U+034F, word joiner U+2060 inserted into identifier fields | Regex | `SKIL-UNI-001` (likely, via general invisible-char detection) | PARTIAL | Not confirmed whether skil's invisible-char set includes U+00AD/U+034F specifically (vs only zero-width space/joiner/BOM); flagged PARTIAL pending confirmation. | — |
| TP2 — Mixed-script identifier | An identifier combines characters from ≥2 Unicode scripts (e.g. Latin + Greek) without literal confusable substitution | `unicodedata.name()` script-prefix detection across the identifier | `SKIL-UNI-002` (`mixedScriptToken` function exists in `unicode.go`) | PARTIAL/FULL (unconfirmed) | skil has a `mixedScriptToken` function suggesting equivalent logic exists, but its exact scope (hostname-only vs any identifier) was not fully read. | — |

## 16. Dependency security / supply chain

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| SC1 — Unpinned Dependencies | Bare package name / `>=` range / `"*"`/`"latest"` in requirements.txt, package.json, pyproject.toml | Regex over dependency-file content | `SKIL-DEP-001` | FULL | — | Documented FULL in crosswalk. |
| SC4 — Known Vulnerable Dependency | Live OSV.dev batch query (Python/npm ecosystems) with an offline static fallback list (15 PyPI + 9 npm CVEs) when OSV is unreachable | Live API + static fallback | `SKIL-DEP-VULN` | PROVIDER_BACKED (documented) | Only runs with `--osv`/`--full`; default scan is offline, matching the reference scanner's own graceful-degradation design (the reference scanner also explicitly reports when it fell back to its static list) | Both tools treat this as opt-in-online / offline-degraded, so this is closer to `FULL`-equivalent-when-enabled than a gap. |
| SC5 — Abandoned Dependencies | Curated set of ~35 known-abandoned PyPI/npm packages (`pycrypto`, `nose`, `request`, `optimist`, ...) | Static set-membership | `SKIL-DEP-ABANDONED` | PROVIDER_BACKED (documented) | skil's built-in seed only covers `pycrypto`; the reference scanner's static list has ~35 entries built in by default with no external provider required | Gap: skil's default-available abandoned-package coverage is much narrower than the reference scanner's out-of-the-box list; extending it does not require a provider. |
| SC6 — Typosquatting | Levenshtein edit-distance (≤2, with a relative-distance guard) against curated popular-package lists (~50 PyPI, ~30 npm) | Edit-distance algorithm | `SKIL-DEP-002` | FULL | — | Documented FULL in crosswalk ("ecosystem-aware canonical package names"). |
| SC7 — Untrusted Container Image | `--disable-content-trust`, `DOCKER_CONTENT_TRUST=0`, `--insecure-registry` | Regex | `SKIL-CONTAINER-TRUST`, `SKIL-BOUNDARY-MUTABLE-IMAGE` | FULL | — | Documented FULL in crosswalk. |
| — (no the reference scanner equivalent) | Multi-ecosystem dependency inventory beyond Python/npm | N/A (the reference scanner's SC1/SC4/SC5/SC6 are PyPI+npm only) | `dependency_inventory.go` — Go, JS, Python, Rust, Ruby, Maven | skil-only | — | Per `native-capabilities.md`, skil's dependency inventory spans 6 ecosystems; the reference scanner's dependency analysis is scoped to `requirements.txt`/`pyproject.toml`/`package.json` only (Python + npm). |

## 17. YARA / malware signatures

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `static_yara.py` YR1-4 (malware, webshell, cryptominer, hack_tool/exploit categories) | Compiles built-in YARA rules (`agent_skills.yar`, `cryptominers.yar`, `hacktools.yar`, `malware.yar.b64`, `webshells.yar`) plus an optional user-supplied rules directory (`--yara-rules-dir`), matches against every file, maps `meta.category` to a rule ID/severity | `yara-python` compile + match, with base64-encoded-rule support and per-rule-file namespace collision handling | `SKIL-YARA-*` | FULL | — | Documented FULL in crosswalk ("independent built-in pack is native; arbitrary external rules remain provider-backed"). the reference scanner's built-in rule set (4 `.yar` files, one custom `agent_skills.yar` targeting webhook-exfil/remote-bootstrap/hidden-instruction/MCP-poisoning/destructive-autonomous-action patterns specifically) versus skil's "native malware pack" were not diffed rule-by-rule — worth a follow-up signature-level comparison, since the reference scanner's `agent_skills.yar` encodes several of the same properties (P3/RA1/TP1/EA2) as YARA signatures in addition to regex, giving it two independent detection paths for the same property that skil covers with only one. |

## 18. Container / infrastructure boundaries

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| SSRF1 (`static_patterns_ssrf.py`) — Cloud metadata endpoint access | `169.254.169.254`, `metadata.google.internal`, `100.100.100.200`, AWS IPv6 IMDS, with a "defensive reference" (documented rejection rule) suppressor | Regex + defensive-context detector | `SKIL-BOUNDARY-METADATA` | FULL | — | skil requires the IP/hostname to co-occur with a metadata/token/credential keyword within 100 chars, which is a tighter (lower-false-positive) version of the same property; the reference scanner's SSRF1 also matches on a bare literal IP with no keyword co-occurrence, which is a stronger recall but the same core property. |
| SSRF2 — Internal/loopback network request | A request-issuing call (`requests.get`, `fetch`, `curl`...) targeting `localhost`/`127.0.0.1`/`10.x`/`192.168.x`/`172.16-31.x` | Regex, request-function-anchored | `SKIL-BOUNDARY-SSRF-INTERNAL` | FULL | — | New rule, deliberately separate from `SKIL-BOUNDARY-SSRF` (which stays scoped to dynamic-untrusted-target evidence) rather than folding into it, since a static hardcoded internal address and an untrusted-variable-built URL are different evidence shapes worth distinguishing in findings. Severity set to Medium (not High) given legitimate local-dev-server request patterns exist; `169.254.169.254` link-local metadata addresses remain the dedicated `SKIL-BOUNDARY-METADATA` rule's responsibility, not double-counted here. |
| SSRF3 — Dynamic/untrusted request target | An f-string/template-literal URL built from an interpolated variable | Regex | `SKIL-BOUNDARY-SSRF` | FULL | — | skil's version is more precise (requires a specific untrusted-source keyword list) vs SSRF3's blanket "any f-string URL" match, which is higher recall but noisier; net the same property. |
| TM4 (`static_patterns_tool_misuse.py`) — Privileged Kubernetes Workload | `privileged: true`, `hostPath:`, `hostPID/hostNetwork/hostIPC: true` in a manifest, `kubectl run --privileged`, Helm privileged override | Regex, code-example filtered | `SKIL-BOUNDARY-CONTAINER-ESCAPE` | FULL | — | skil folds K8s and Docker privilege-escape signals into one rule; property coverage matches. |

## 19. Agent snooping (`.claude`/`.codex`/`.cursor` directories)

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `static_patterns_agent_snooping.py` AS1 — Agent config directory access | Reading `.claude/`, `.codex/`, `.gemini/`, `.continue/` config/settings/credentials files via `open()`, `Path()`, or shell `cat`/`grep` | Regex, 6 patterns | `SKIL-BOUNDARY-AGENT-STATE` | PARTIAL | skil's rule lists `.codex/.claude/.cursor` plus generic "agent history/memory/session/transcript" and "conversation history/log" nouns; it does **not** include `.gemini` or `.continue`, both of which AS1 explicitly covers, and does not explicitly anchor on config/settings/credentials *file* access the way AS1 does (skil's pattern is verb+noun-generic, not path-specific for the config-file case) | `.cursor` is skil-only (AS1 doesn't cover Cursor); `.gemini`/`.continue` are AS1-only. Net: overlapping but non-identical directory coverage. |
| AS2 — MCP config file access | Direct access to `mcp.json`/`mcp_config.json`, or "list all available MCP servers", or reading API keys embedded in MCP config | Regex, 7 patterns | `SKIL-BOUNDARY-MCP-CONFIG` | FULL | — | Implemented in `boundary.go`: matches direct `mcp(_config).json` file access (via `open`/`cat`/`grep`/etc. or a `.codex`/`.claude`/`.gemini`/`.cursor`-prefixed path), MCP server/tool enumeration instructions, and config-plus-secret-keyword co-occurrence. Verified negative: a skill's own MCP tool description does not fire. |
| AS3 — Skill enumeration/snooping | `os.listdir`/`glob.glob` over `.claude/skills`/`.codex/skills`, reading another skill's `SKILL.md`, "enumerate all installed skills" | Regex, 6 patterns | `SKIL-BOUNDARY-PEER-SKILL` | FULL | — | Implemented in `boundary.go`: matches sibling-skill-directory enumeration (code or shell) and natural-language instructions to enumerate/read other installed skills. |

## 20. MCP security

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `mcp_least_privilege.py` LP1 — Under-declared capability | Code capability (shell/network/file-read/file-write/env/mcp) detected but not covered by declared `permissions`/`allowed-tools`, with test-file confidence discount | Regex-based capability detector + declared-permission-to-category mapping, deterministic (no contract needed) | `SKIL-CAP-DECLARATION-MISSING` | DIFFERENT_BY_DESIGN | skil's analog only fires when *no* skill contract exists at all (`verify`-only, per `external-control-crosswalk.md`: "advisory only; actual mismatch remains a verification concern"); it does not do the reference scanner's heuristic manifest-declared-vs-code-detected capability diff during a plain scan | Architectural difference: the reference scanner infers under-declaration heuristically from the manifest at scan time; skil requires an explicit reviewed contract (`skil verify`) to make the same determination precisely, trading heuristic default-on coverage for contract-bound precision. |
| LP2 — Wildcard permission | `permissions: ["*"/"all"/"full"/"any"]` | Set-membership check | `SKIL-MCP-001`, `SKIL-AGENCY-TOOLS` | FULL | — | — |
| LP3 — No permissions declared, capabilities detected | No `permissions`/`allowed-tools` field at all, yet code capabilities exist | Deterministic, manifest-absence + capability-detection | `SKIL-CAP-DECLARATION-MISSING` | FULL | — | Documented FULL in crosswalk. |
| LP4 — Over-declared permission | Declared permission has no corresponding code capability | Deterministic, permission-to-capability reverse mapping | `verification.Verify`'s existing `overdeclared` mechanism, now extended to `filesystem.read` and `environment.read` | PARTIAL | Category-level over-declaration (scalar/array-length capabilities: network, commands, filesystem read/write/delete, secrets, environment, persistence, external side effects) is now fully covered via `skil verify`. Per-item over-declaration within an allowlist (e.g. `tools.allow: [bash, curl]` where only `bash` is ever used) is still not covered — no analyzer currently observes *which* declared tool/MCP-server/MCP-tool array entries were actually exercised, so adding that check now would reproduce the H35-class false-positive bug (flagging legitimately unused-but-declared items with no positive observation source to disprove it). | This required real new observation plumbing, not just a new `overdeclared()` call: `filesystem.read` and `environment.read` had zero capability-observation coverage from any analyzer before this change (an `open(path, "r")` or `os.getenv(name)` call produced no observation at all), so `PythonAST.AnalyzeCapabilities` now emits `filesystem.read`/`environment.read` observations for read-mode `open()` and environment-variable reads respectively. |
| `mcp_rug_pull.py` RP1 — Unpinned MCP server / artifact reference | `npx`/`uvx`/`pip install`/`docker pull` without a version pin or digest | Regex per-command-family | `SKIL-MCP-003`, `SKIL-BOUNDARY-MUTABLE-IMAGE` | FULL | — | Documented FULL in crosswalk. |
| RP1 (manifest-diff form) — Permission expansion between versions | Comparing `permissions` field of current vs. a supplied `previous_manifest` | Structured diff, requires two manifest snapshots | `SKIL-MCP-005` (lock-file diff, different mechanism) | DIFFERENT_BY_DESIGN | skil's rug-pull detector (`SKIL-MCP-005`) diffs MCP tool *metadata* against a `.skil/mcp-tools.lock.json` reviewed lock file, not the skill manifest's declared `permissions` field against an arbitrary prior manifest snapshot the caller supplies; different data source, same underlying "did trust surface change since last review" property | — |
| RP2 — Trigger phrase modification between versions | Comparing `triggers` field of current vs. previous manifest | Structured diff | none found with this specific mechanism | MISSING | skil has no manifest-version-diff detector for trigger changes; `SKIL-TRIGGER-SHADOW`/`SKIL-TRIGGER-GENERIC` evaluate triggers in isolation, not against a prior reviewed version. | — |
| RP2 (static form) — Permission pre-staging language | "Add new/additional/extra permissions" language in the manifest, suggesting future expansion | Regex over manifest text | `SKIL-MANIFEST-PERMISSION-STAGING` | FULL | — | — |
| RP3 — Parameter schema/default modification between versions | Comparing `parameters` type/default/description across manifest versions | Structured diff | `SKIL-MCP-005` (partial, via lock-file diff covering tool metadata broadly) | PARTIAL | — | — |
| RP3 (static form) — Unpinned skill version (`*`/`latest`/`>=`/`^`) | Manifest `version` field is a wildcard or overly broad range | Regex/string check | `SKIL-MANIFEST-UNPINNED-VERSION` | FULL | — | Line-scoped regex distinguishes the skill's own declared version from the contract schema's integer `version` field without needing structured YAML parsing (an integer can never match the wildcard/range token set). |
| `mcp_tool_poisoning.py` TP1 — Hidden instructions in MCP metadata | HTML/Markdown comments, zero-width chars, base64 blobs, data URIs embedded in `name`/`description`/`triggers`/parameter fields | Regex, scoped to parsed manifest metadata text fields | `SKIL-MCP-002` (poison-keyword regex only) | PARTIAL | `SKIL-MCP-002`/`poisonValue` regex matches literal phrases ("ignore previous/system", "never refuse", "reveal system/secret", "hidden instruction") inside `description`/`default` fields; it does not check MCP metadata fields for HTML comments, zero-width chars, base64 blobs, or data URIs the way TP1 does — skil's general `SKIL-UNI-*`/`SKIL-OBF-001` rules run over file text broadly and may incidentally catch some of this, but are not scoped to MCP metadata fields specifically | Worth confirming whether `SKIL-UNI-*` fires on MCP JSON/YAML metadata files at all (their `AppliesTo` in `unicode.go` wasn't confirmed) — flagged PARTIAL. |
| TP2 — Unicode deception in MCP identifiers | Homoglyph/RTL/invisible/mixed-script identifiers in tool `name`/`triggers`/parameter names | Curated confusables + script detection, identifier-scoped | `SKIL-UNI-002` | PARTIAL | See Unicode-deception section above — scope-to-MCP-identifiers not confirmed. | — |
| TP3 — Parameter description injection | Instruction-override phrases, system-prompt tokens (`SYSTEM:`, `<system>`, `[INST]`), exfiltration phrases, or malicious URLs/shell commands in a parameter's `description`/`default`, plus an excessive-length (>500 char) check | Regex, scoped to `parameters[].description`/`.default` | `SKIL-MCP-004` | PARTIAL | `SKIL-MCP-004`'s description says it targets "parameter description requests secrets or credentials" — narrower than TP3's four sub-checks (instruction-override, system-token, exfiltration phrase, malicious-default-value, excessive length); the exact regex was not read in this pass, so full overlap is unconfirmed | — |
| TP4 — LLM description-behavior mismatch (MCP-specific) | LLM call comparing manifest description/triggers/permissions against actual code | LLM semantic check, always attempted by default (`use_llm=True`) | `SKIL-MCP-006`, `SKIL-INTENT-DESCRIPTION` | PARTIAL/PROVIDER_BACKED | skil's equivalents are provider-backed (require an LLM provider to be configured) whereas the reference scanner's TP4 defaults to on for any scan (assuming an LLM is configured at all, which the reference scanner also requires); functionally similar but skil's default scan (no provider) emits nothing for this property, while the reference scanner's CLI defaults `use_llm=True` | — |

## 21. Semantic / LLM-based analysis (developer intent, quality/policy, security discovery)

| Reference | Property | Approach | skil equivalent | Coverage | Gap | Notes |
|---|---|---|---|---|---|---|
| `semantic_developer_intent.py` SDI-1 — Description-behavior mismatch | LLM compares manifest description vs. code operations | LLM prompt, per-file batches | `SKIL-INTENT-DESCRIPTION` (provider-backed) | PARTIAL | — | — |
| SDI-2 — Context-inappropriate capability | LLM flags a capability unjustified by the stated purpose | LLM prompt | `SKIL-INTENT-CONTEXT` (provider-backed) | PARTIAL | — | — |
| SDI-3 — Scope creep vs. declared permissions | LLM compares code behavior against declared `permissions` | LLM prompt | `SKIL-INTENT-SCOPE` (provider-backed) | PARTIAL | — | — |
| SDI-4 — Intent-code divergence (docstring contradicts code) | LLM flags comments/docstrings that actively contradict the code | LLM prompt | `SKIL-INTENT-IMPLEMENTATION` | FULL (deterministic, per `rules.go` engine map: `builtin.local-semantic`) | — | Notably, skil implements this *specific* property (SDI-4) as a **deterministic, non-LLM** local-semantic pass ("builtin.local-semantic" per `NativeControlImplementations`), while the reference scanner requires an LLM call for it — skil is structurally stronger here (works offline, no provider dependency) if `local_semantic.go`'s actual detection quality matches the LLM's. |
| `semantic_quality_policy.py` SQP-1 — Vague triggers | LLM flags ambiguous/overbroad trigger phrasing in markdown/manifest files | LLM prompt, file-type-scoped | `SKIL-TRIGGER-GENERIC`, `SKIL-TA-001` (deterministic regex, not LLM) | PARTIAL/DIFFERENT_BY_DESIGN | skil's deterministic pattern-based triggers cover the same property without needing an LLM, but with narrower phrasing recall (see Trigger Abuse section) | skil trades LLM's broad natural-language judgment for a deterministic regex — different mechanism, overlapping but not equivalent coverage. |
| SQP-2 — Missing user warnings | LLM flags safety-critical operations (file writes, network calls, credential access, subprocess, destructive ops) with no user-facing disclosure | LLM prompt, differing criteria for code vs. markdown files | `SKIL-INTENT-UNDISCLOSED-OPERATION` | PARTIAL | New deterministic rule, but it is a **positive-signal proxy**, not a true absence-of-disclosure detector: it fires when instructions explicitly frame a dangerous operation as silent/secret/covert/without-consent, rather than by checking every dangerous operation for a nearby disclosure and flagging the ones missing one. This trades recall (an operation that is simply undisclosed, with no explicit "silently" framing, is not caught) for precision (near-zero false-positive risk, fully deterministic, no LLM dependency) — the true absence-checking form of SQP-2 is architecturally an LLM-judgment task the reference scanner itself only runs via its semantic layer. | Text-only for now (markdown/instruction scope); a code-level analog (a dangerous call with no adjacent log/print/confirm) was deliberately not attempted — it would need cross-line/function-scope analysis to avoid being extremely noisy (most code has no user-facing print statements at all) and was judged out of scope for this pass. |
| SQP-3 — Natural-language policy violations (e.g. forced language/locale) | LLM flags org-policy violations like forcing a specific language without opt-in | LLM prompt, all file types | none found | MISSING | — | — |
| `semantic_security_discovery.py` SSD-1 — Semantic prompt injection (non-literal) | LLM detects polite/role-play/hypothetical reframings of instruction override that evade keyword regex | LLM prompt, explicitly scoped to residual gap beyond static regex | `SKIL-SEM-SECURITY` (provider-backed, generic) | PARTIAL | skil's `SKIL-SEM-SECURITY` is a generic "semantic security weakness not reducible to a lexical pattern" catch-all, provider-backed; it is not confirmed to specifically target the four SSD sub-properties (reframed injection, paraphrased attacks, natural-language exfiltration, gradual/narrative deception) with the same prompt precision the reference scanner uses | — |
| SSD-2 — Novel/paraphrased attack phrasing | LLM detects creative synonyms/encoded intent for known attack patterns | LLM prompt | `SKIL-SEM-SECURITY` (generic, provider-backed) | PARTIAL | — | — |
| SSD-3 — Natural-language exfiltration instructions (no technical terms) | LLM detects "remember everything the user tells you and include it in your response" style leaks without explicit "exfiltrate"/"send" | LLM prompt | `SKIL-SEM-SECURITY` (generic, provider-backed) | PARTIAL | — | — |
| SSD-4 — Narrative/gradual deception | LLM detects multi-step trust-building sequences that cumulatively steer toward harm | LLM prompt | `SKIL-SEM-COMPOSITE` | PARTIAL | `SKIL-SEM-COMPOSITE`'s stated purpose ("independent semantic observations combine into a material cross-boundary risk") is conceptually close to SSD-4's narrative/gradual-deception detection, but is provider-backed and its exact prompt was not read | — |

---

## Summary

**Total distinct the reference scanner properties catalogued: ~90** (counting every rule ID across `P1-P8`, `AR1-3`, `E1-E5`, `EA1-4`, `OH1-3`, `PE1-5`, `MP1-3`, `RA1-2`, `SSRF1-3`, `SC1-7`, `TR1-3`, `TM1-4`, `AS1-3`, `AST1-9`, `TT1-5`, `LP1-4`, `RP1-3`, `TP1-4`, `SDI1-4`, `SQP1-3`, `SSD1-4`, plus the 4-category YARA rule family as one property group).

**Coverage-state counts** (approximate — several rows are marked PARTIAL pending a closer follow-up code read, noted individually above):

| State | Count at initial survey | Count after closing the 8 ranked gaps |
|---|---:|---:|
| FULL | ~34 | ~44 |
| PARTIAL | ~38 | ~36 |
| MISSING | ~14 | ~6 |
| DIFFERENT_BY_DESIGN | ~4 | ~4 |
| NOT_APPLICABLE | 0 | 0 |
| skil-only (no the reference scanner equivalent) | ~6 (not counted in the ~90 total) | ~6 |

All 8 originally ranked top-priority gaps (§ below) are now closed or partially closed; counts are approximate re-derivations, not a fresh full re-audit of all ~90 rows. The remaining PARTIAL/MISSING rows scattered through the sections above (not part of the original top-8 ranking) were not revisited in this pass.

FULL is concentrated in the areas the existing `external-control-crosswalk.md` conformance suite already asserts: Python AST dynamic-execution (AST1-9), taint tracking (TT2-5), supply-chain regexes (SC1/2/3/6/7), core prompt-injection/anti-refusal/agency instruction patterns, credential-path access, and YARA. MISSING and DIFFERENT_BY_DESIGN cluster almost entirely in three places: (1) MCP-specific hygiene properties that need a parsed-manifest-diff mechanism skil's MCP analyzer doesn't have (LP4, RP2, RP3-static, manifest version-diff), (2) agent/skill-snooping properties beyond `.claude`/`.codex`/`.cursor` config access (AS2, AS3), and (3) semantic properties the reference scanner always runs by LLM default that skil only runs when a semantic provider is explicitly configured (SQP-2, SQP-3, and the finer-grained SDI/SSD sub-properties collapse into skil's generic `SKIL-SEM-*` catch-alls).

### Top priority gaps (ranked by security impact and breadth of zero-coverage, not ease of implementation)

1. ~~**MCP snooping and hygiene properties (AS2, AS3, LP4, RP2, RP3-static, manifest trigger/permission version-diff)**~~ — **Partially closed.** AS2 (`SKIL-BOUNDARY-MCP-CONFIG`), AS3 (`SKIL-BOUNDARY-PEER-SKILL`), RP2-static (`SKIL-MANIFEST-PERMISSION-STAGING`), and RP3-static (`SKIL-MANIFEST-UNPINNED-VERSION`) are now implemented and FULL; LP4's category-level over-declaration is now covered for `filesystem.read`/`environment.read` (see LP4 row above for the remaining per-item-allowlist gap). **Still MISSING**: the true manifest-*diff* mechanisms (RP2/RP3 comparing a current manifest against a supplied prior snapshot, and the equivalent trigger/permission-expansion-between-versions signal) — these need a new `--previous-manifest` comparison surface, which is a distinct, larger feature (new CLI input, diff logic, tests) deliberately not attempted in this pass alongside the static-form detectors.
2. ~~**PE2 (sudo/root shell invocation) and the exotic PE5 container-escape primitives**~~ — **Closed.** `SKIL-SH-002` extended to `doas`/`pkexec`/`su -`/`chmod u+s`; `SKIL-BOUNDARY-CONTAINER-ESCAPE` extended to `nsenter`, `unshare`, cgroup `release_agent`, and `--cap-add=SYS_ADMIN`/`SYS_PTRACE`/`NET_ADMIN`.
3. ~~**SSRF2 (static internal/loopback-target request)**~~ — **Closed.** New `SKIL-BOUNDARY-SSRF-INTERNAL` rule matches hardcoded `127.x`/`10.x`/`192.168.x`/`172.16-31.x`/`localhost` request targets, kept separate from `SKIL-BOUNDARY-SSRF`'s dynamic-taint evidence.
4. ~~**P4 (subtle behavior-manipulation / dark-pattern steering)**~~ — **Closed.** New `SKIL-INTENT-BEHAVIOR-MANIPULATION` rule.
5. ~~**SQP-2 (missing user-disclosure of dangerous operations)**~~ — **Partially closed.** New `SKIL-INTENT-UNDISCLOSED-OPERATION` rule catches the explicit "do this silently/secretly/without consent" framing deterministically; the harder true-absence-of-disclosure form remains an LLM-shaped judgment task, honestly left PARTIAL rather than faked with a noisy heuristic.
6. ~~**E5/cloud-exfiltration via SDK calls**~~ — **Closed.** New `SKIL-BOUNDARY-CLOUD-SDK-UPLOAD` rule.
7. ~~**PE3 path-list breadth**~~ — **Closed.** `SKIL-SEC-001` extended (see row above).
8. ~~**E3 code-level filesystem enumeration**~~ — **Closed.** New `SKIL-FS-DISCOVERY-CODE` rule.
